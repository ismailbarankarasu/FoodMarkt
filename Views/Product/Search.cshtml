@model List<FoodMart.Dtos.ProductDtos.ResultProductDto>

@{
    ViewData["Title"] = "Ürün Ara";

    var searchTerm = ViewBag.SearchTerm as string ?? "";
}

<section class="py-5">
    <div class="container-fluid">

        <div class="row justify-content-center mb-5">
            <div class="col-lg-8">

                <div class="text-center mb-4">

                    <h1 class="section-title">
                        Ürün Ara
                    </h1>

                    <p class="text-muted">
                        Aradığınız ürünü hızlıca bulun.
                    </p>

                </div>

                <form asp-controller="Product"
                      asp-action="Search"
                      method="get">

                    <input type="hidden" name="categoryId" value="@ViewBag.CategoryId" />
                    <div class="input-group input-group-lg">

                        <label for="productSearchInput" class="visually-hidden">Ürün ara</label>
                        <input id="productSearchInput" type="search"
                               name="search" maxlength="150"
                               value="@searchTerm"
                               class="form-control"
                               placeholder="Ürün adı veya açıklama ara..."
                               autocomplete="off" />

                        <button type="submit"
                                class="btn btn-primary px-4">

                            <i class="bi bi-search"></i>
                            Ara

                        </button>

                    </div>

                </form>

            </div>
        </div>


        @if (Model.Any())
        {
            <div class="d-flex justify-content-between align-items-center mb-4">

                <div>
                    <h3 class="mb-1">
                        Arama Sonuçları
                    </h3>

                    <p class="text-muted mb-0">
                        @Model.Count ürün bulundu.
                    </p>
                </div>

            </div>
        }


        @if (Model.Any())
        {
            <div class="row g-4">

                @foreach (var item in Model)
                {
                    var hasDiscount =
                        item.DiscountPrice.HasValue &&
                        item.DiscountPrice.Value > 0 &&
                        item.DiscountPrice.Value < item.Price;

                    var discountRate = 0;

                    if (hasDiscount && item.Price > 0)
                    {
                        discountRate = (int)Math.Round(
                            (1 - (item.DiscountPrice!.Value / item.Price)) * 100);
                    }

                    <div class="col-xl-3 col-lg-4 col-md-6">

                        <div class="product-item h-100">

                            @if (hasDiscount)
                            {
                                <span class="badge bg-success position-absolute m-3">
                                    -%@discountRate
                                </span>
                            }

                            <figure>

                                <a asp-controller="Product" asp-action="Detail" asp-route-id="@item.Id" title="@item.Name">

                                    <img src="@item.ImageUrl"
                                         alt="@item.Name"
                                         class="tab-image" />

                                </a>

                            </figure>

                            <h3>
                                @item.Name
                            </h3>

                            <div class="mb-2">

                                <span class="qty">
                                    @item.CategoryName
                                </span>

                            </div>

                            @if (item.Stock > 0)
                            {
                                <span class="text-success small">
                                    Stokta
                                </span>
                            }
                            else
                            {
                                <span class="text-danger small">
                                    Stokta Yok
                                </span>
                            }

                            <div class="mt-2 mb-3">

                                @if (hasDiscount)
                                {
                                    <span class="price">
                                        @item.DiscountPrice!.Value.ToString("N2", System.Globalization.CultureInfo.GetCultureInfo("tr-TR")) ₺
                                    </span>

                                    <span class="text-muted text-decoration-line-through ms-2">
                                        @item.Price.ToString("N2", System.Globalization.CultureInfo.GetCultureInfo("tr-TR")) ₺
                                    </span>
                                }
                                else
                                {
                                    <span class="price">
                                        @item.Price.ToString("N2", System.Globalization.CultureInfo.GetCultureInfo("tr-TR")) ₺
                                    </span>
                                }

                            </div>

                            <a asp-controller="Product" asp-action="Detail" asp-route-id="@item.Id" class="btn btn-outline-dark mt-3">Ürünü incele</a>

                        </div>

                    </div>
                }

            </div>
        }
        else if (!string.IsNullOrWhiteSpace(searchTerm))
        {
            <div class="text-center py-5">

                <div class="mb-3">
                    <i class="bi bi-search fs-1 text-muted"></i>
                </div>

                <h3>
                    Ürün bulunamadı
                </h3>

                <p class="text-muted">
                    "<strong>@searchTerm</strong>"
                    aramasına uygun aktif bir ürün bulunamadı.
                </p>

                <a asp-controller="Default"
                   asp-action="Index"
                   class="btn btn-primary mt-2">

                    Ana Sayfaya Dön

                </a>

            </div>
        }

    </div>
</section>